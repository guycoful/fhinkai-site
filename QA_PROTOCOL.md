# פרוטוקול בדיקות מחמיר - מערכת חיה (מקפצה)

עיקרון על: בדיקת "ה-endpoint חי ומחזיר 200" אינה בדיקה. מערכת יכולה להחזיר 200 ולשמור נתונים שגויים, לאבד פרמטרים ב-redirect, או לתייג קבוצה לא נכונה. כל בדיקה חייבת לאמת **התנהגות נכונה מקצה לקצה ועל בסיס נתונים אמיתי**, לא חיוּת בלבד.

## כללי ברזל

1. תבדוק את כל מסע המשתמש מקצה לקצה, לא endpoint בודד. כל קישור כניסה (כל קוהורט) נבדק מהקליק הראשון ועד שורת ה-DB. מסך הצלחה אינו הוכחה, רק שורה נכונה ב-DB היא הוכחה.

2. תאמת ערכים, לא רק שמירה. אחרי כל submission בדיקה, תשלוף את השורה מה-DB ותוודא שכל שדה תואם לקלט: cohort, source, שם, טלפון, וכל שדה קריטי. "נשמר 200" שונה מ"נשמר נכון".

3. תבדוק שכל פרמטר שורד כל redirect. כל פרמטר שמשנה התנהגות (cohort, src, pid, lead) נבדק בכל קפיצה בשרשרת רב-עמודית. redirect הוא המקום מספר אחת שבו פרמטרים נופלים בשקט. תאמת את ה-URL בכל hop.

4. תבדוק כל קוהורט וכל וריאנט, לא רק ברירת המחדל. הבאג מתחבא כי בדקו תמיד את pilot. כל קישור קוהורט נבדק במפורש בנפרד, כולל lms ו-rehearsal, ולא רק הזרימה הדיפולטית.

5. תצליב בין טבלאות. יחסים שאמורים להתקיים נבדקים: לידים מול משתתפים לפי טלפון, ספירה לכל קוהורט, יתומים מסומנים. פער בין הטבלאות הוא דגל, לא רעש.

6. תבדוק דרך ה-URL החי הפרוס, בדפדפן אמיתי, עם רשומת בדיקה מסומנת (source=e2e_test או טלפון בדיקה ייעודי), ואז תנקה אותה. בדיקה מקומית בלבד לא תופסת בעיות deploy, cache, ו-redirect מוחלט.

7. תבדוק מקרי קצה ושליליים: חיתוך זמן רישום (late_registration), התנהגות ברירת מחדל כשפרמטר חסר, וטיפול בטלפון כפול (upsert מול insert).

8. תריץ אחרי כל deploy וגם בלוח זמנים קבוע. לא ping, אלא מסע מלא לכל קוהורט.

9. תיכשל בקול. כל פער בין הצפוי לבין מצב ה-DB בפועל = התראה, לא כשל שקט. הבדיקה מדפיסה את הצפוי מול בפועל לכל שדה.

## צ'ק-ליסט מינימלי לפני כל deploy חי

- [ ] כל קישור קוהורט (pilot, lms, rehearsal) נפתח בדפדפן ועבר עד day1, וה-URL בכל שלב נשא את ה-cohort הנכון.
- [ ] submission בדיקה לכל קוהורט יצר שורה ב-DB עם cohort ו-source נכונים, ואומת בשליפה.
- [ ] לידים מול משתתפים הצטלבו לפי טלפון, אין יתומים לא מוסברים.
- [ ] חיתוך הזמן (late_registration) נבדק: לפני הפתיחה דוחה, אחרי הפתיחה מקבל.
- [ ] רשומות הבדיקה נמחקו אחרי הבדיקה.
